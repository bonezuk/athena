set(CMAKE_INCLUDE_CURRENT_DIR ON)
set(CMAKE_AUTOMOC ON)

message ("Qt path")

set(QTVER "Qt6")

if (OMEGA_WIN32)
	set(QT_HOME "" CACHE PATH "Qt install directory")
	set(CMAKE_PREFIX_PATH "${QT_HOME}")
else (OMEGA_WIN32)
	string(STRIP "${CMAKE_PREFIX_PATH}" CMAKE_PREFIX_PATH)
	if (CMAKE_PREFIX_PATH STREQUAL "")
		message("Qt - 1")
		set(QT_HOME "" CACHE PATH "Qt install directory")
		set(CMAKE_PREFIX_PATH "${QT_HOME}")
	else (CMAKE_PREFIX_PATH STREQUAL "")
		message("Qt - 2")
		set(QT_HOME "${CMAKE_PREFIX_PATH}")
	endif (CMAKE_PREFIX_PATH STREQUAL "")
	message("${QT_HOME}")
	message("${CMAKE_PREFIX_PATH}")
endif (OMEGA_WIN32)

set(QT_HOME2 "${QT_HOME}")

find_package(Qt6Core)
find_package(Qt6Gui)
find_package(Qt6Xml)
find_package(Qt6Widgets)
find_package(Qt6Test)
find_package(Qt6Qml)
find_package(Qt6Quick)
find_package(Qt6DBus)
find_package(Qt6Core5Compat)
find_package(Qt6Network)

if (${TIGER_LINUX_DISTRO})
	set(QT_HOME "${ATHENA_UTILS}/usr")
endif (${TIGER_LINUX_DISTRO})

include_directories(AFTER "${QT_HOME}/include" )
include_directories(AFTER "${QT_HOME}/include/QtCore" )
include_directories(AFTER "${QT_HOME}/include/QtGui" )
include_directories(AFTER "${QT_HOME}/include/QtXml" )
include_directories(AFTER "${QT_HOME}/include/QtWidgets" )
include_directories(AFTER "${QT_HOME}/include/QtTest" )
include_directories(AFTER "${QT_HOME}/include/QtQml" )
include_directories(AFTER "${QT_HOME}/include/QtQuick" )
include_directories(AFTER "${QT_HOME}/include/QtDBus" )
include_directories(AFTER "${QT_HOME}/include/QtCore5Compat" )
include_directories(AFTER "${QT_HOME}/include/QtNetwork" )

if (OMEGA_WIN32)

	if (TIGER_DEBUG_BUILD)
		message("Qt Set Debug")
		set(QT_CORE_LIBNAME "Qt6Cored")
		set(QT_GUI_LIBNAME "Qt6Guid")
		set(QT_XML_LIBNAME "Qt6Xmld")
		set(QT_WIDGETS_LIBNAME "Qt6Widgetsd")
		set(QT_TEST_LIBNAME "Qt6Testd")
		set(QT_QML_LIBNAME "Qt6Qmld")
		set(QT_QUICK_LIBNAME "Qt6Quickd")
		set(QT_NETWORK_LIBNAME "Qt6Networkd")
		set(QT_DBUS_LIBNAME "Qt6DBusd")
		set(QT_CORE5COMPACT_LIBNAME "Qt6Core5Compatd")
		set(QT_NETWORK_LIBNAME "Qt6Networkd")
		
	else (TIGER_DEBUG_BUILD)
		set(QT_CORE_LIBNAME "Qt6Core")
		set(QT_GUI_LIBNAME "Qt6Gui")
		set(QT_XML_LIBNAME "Qt6Xml")
		set(QT_WIDGETS_LIBNAME "Qt6Widgets")
		set(QT_TEST_LIBNAME "Qt6Test")
		set(QT_QML_LIBNAME "Qt6Qml")
		set(QT_QUICK_LIBNAME "Qt6Quick")
		set(QT_NETWORK_LIBNAME "Qt6Network")
		set(QT_DBUS_LIBNAME "Qt6DBus")
		set(QT_CORE5COMPACT_LIBNAME "Qt6Core5Compat")
		set(QT_NETWORK_LIBNAME "Qt6Network")
		
	endif (TIGER_DEBUG_BUILD)

	if (OMEGA_MSVC12 OR OMEGA_MSVC16)
		if (TIGER_DEBUG_BUILD)
			set(LIB_EGL_LIBNAME "libEGLd")
			set(LIB_GLES_LIBNAME "libGLESv2d")
		else (TIGER_DEBUG_BUILD)
			set(LIB_EGL_LIBNAME "libEGL")
			set(LIB_GLES_LIBNAME "libGLESv2")
		endif (TIGER_DEBUG_BUILD)
	endif (OMEGA_MSVC12 OR OMEGA_MSVC16)
	
elseif (OMEGA_MACOSX)

	set(QT_CORE_LIBNAME "libQt6Core")
	set(QT_GUI_LIBNAME "libQt6Gui")
	set(QT_XML_LIBNAME "libQt6Xml")
	set(QT_WIDGETS_LIBNAME "libQt6Widgets")
	set(QT_TEST_LIBNAME "libQt6Test")
	set(QT_PRINTSUPPORT_LIBNAME "libQt6PrintSupport")
	set(QT_QML_LIBNAME "libQt6Qml")
	set(QT_QUICK_LIBNAME "libQt6Quick")
	set(QT_NETWORK_LIBNAME "libQt6Network")
	set(QT_DBUS_LIBNAME "libQt6DBus")
	set(QT_CORE5COMPACT_LIBNAME "libQt6Core5Compat")
	set(QT_NETWORK_LIBNAME "libQt6Network")

elseif (OMEGA_LINUX)

	set(QT_CORE_LIBNAME "libQt6Core")
	set(QT_GUI_LIBNAME "libQt6Gui")
	set(QT_XML_LIBNAME "libQt6Xml")
	set(QT_WIDGETS_LIBNAME "libQt6Widgets")
	set(QT_TEST_LIBNAME "libQt6Test")
	set(QT_QML_LIBNAME "libQt6Qml")
	set(QT_QUICK_LIBNAME "libQt6Quick")
	set(QT_NETWORK_LIBNAME "libQt6Network")
	set(QT_DBUS_LIBNAME "libQt6DBus")
	set(QT_CORE5COMPACT_LIBNAME "libQt6Core5Compat")
	set(QT_NETWORK_LIBNAME "libQt6Network")

endif (OMEGA_WIN32)
